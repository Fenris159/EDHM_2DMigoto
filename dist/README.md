# dist/

Drop built DLLs and release packages here for local testing or packaging.

Contents of this directory (except this README / `.gitkeep`) are **gitignored by default**. Commit artifacts only when you intentionally want them in the repo.
