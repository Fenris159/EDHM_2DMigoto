# Scripts

| Script | Description |
|--------|-------------|
| `fetch-upstream.ps1` | Fetch `xxmi` / `3dmigoto` remotes; optional summary of new XXMI commits |

```powershell
.\scripts\fetch-upstream.ps1
.\scripts\fetch-upstream.ps1 -ShowNew
```
